# ワタセル Phase 1 Webアプリ 要件一覧

最終更新: 2026-09-12

この文書は、**同じフロントで本番の Web アプリを組むときの実装要件**である。
プロダクト思想・テンプレート・禁止事項の正本は [`DESIGN.md`](./DESIGN.md)。
データ形の正本は [`schema/work.schema.json`](./schema/work.schema.json)。
エージェント指示の正本は [`prompts/`](./prompts/)。
凍結した画面の正本ソースは [`frontend/`](./frontend/)。

画面の見た目・情報設計・文言は、現行プロトタイプ（=`frontend/`）を正本とする。**フロントを作り直さない。**

| 文書 | 役割 |
| --- | --- |
| このファイル | 何を作るか / 何は既にあるか / 何を足すか |
| `DESIGN.md` | なぜ作るか、取材手順、成果物テンプレ、品質ゲート |
| `frontend/` | 画面・トークン・操作の完成見本（コピーして使う） |

状態の記号:

- **済** … プロトタイプにあり、本番でも同じ挙動にする
- **P0** … 社外に出す前に必須
- **P1** … Phase 1（録音→文字起こし→成果物）の完成に必要
- **P2** … Phase 2 以降（業種パック、席を超えた運用）
- **しない** … 設計上やらない

---

## 0. 固定するフロント

作り直さない。色もフォントも余白も、現行のまま移植する。

### 0.1 画面（5つだけ）

| 画面 | 経路 | 役割 |
| --- | --- | --- |
| 始める | `/` | 現場の呼び名を1つ。ボタンは「始める」だけ |
| 取材 | `/interview/:workId` | 会話。マイク。使い道は静かな select。ヘッダ右「出す」 |
| 一覧 | `/works` | 1行＝1業務。状態は「済 / 穴 / 途中」 |
| 成果物 | `/works/:workId` | 紙面。下線タブ。要確認から穴埋め。末尾に取材・印刷・書き出す |
| 印刷 | `/print/:workId` | 必須成果物を縦に積む |

ホームに使い道チップ、素材カード、最近の業務を置かない。
成果物にカード枠と二重タブを置かない。
一覧の各行に削除を出さない（削除は成果物の末尾）。

### 0.2 トークン

| 役割 | 値 |
| --- | --- |
| 紙 | `#f3efe6` |
| 紙の上 | `#faf7f1` |
| 墨 | `#1c1917` |
| 補助 | `#6b6560` |
| 苔 | `#2f4a3c` |
| 苔の上 | `#f3efe6` |
| 判子（危険） | `#8b3a2f` |
| 注意 | `#8a5a28` |
| 罫 | `#d4ccbc` |
| 見出し | Shippori Mincho / 游明朝 |
| 本文 | IBM Plex Sans JP / ヒラギノ角ゴ |
| タップ | 高さ 44px 以上 |
| 本文サイズ | 16px（iOS のズーム防止） |

絵文字アイコン禁止。紫禁止。グラデーション禁止。カードの山禁止。

### 0.3 コピー（変えない）

- 一言: 採用の前に、仕事を言葉にする。
- ホーム見出し: 仕事を、渡せる言葉に。
- ホーム補助: 現場の呼び名をひとつ。
- 第一声（名前なし）:

```
ワタセルを開始します。頭の中の仕事を、他人に渡せる言葉にします。

今回、渡したい仕事を現場の呼び方で1つください。
```

- 取材プレースホルダ: 一番ふつうの1件から
- タブ略称: カード / SOP / リスト / 発注 / 仕分け / 品質 / 求人 / AI / ログ
- 使い道: 新人教育 / 外注 / 頭の整理 / 求人 / AI化準備
- 空欄タグ: `[要確認]` `[推論]`（隠さない）

### 0.4 ナビ

- スマホ: 下タブ「始める / 一覧」。取材と印刷では隠す
- デスクトップ: ヘッダ右に同じ2語
- 取材中は戻る矢印 + 業務名 + 「出す」
- ノッチとホームインジケータを避ける（`viewport-fit=cover`、safe-area）

---

## 1. プロトタイプで既にできていること（済）

本番でもこの挙動を壊さない。

1. 現場の呼び名を入れて取材を開始する（空でも開始可）
2. 取材は最大3問ずつ。例外から入らせない
3. Web Speech API（ja-JP）で話す。非対応なら書いて進む
4. 「出す」で業務オブジェクトを正規化し、第8章テンプレで Markdown を書く
5. 使い道ごとに必須成果物が変わる

   | 使い道 | 必須 |
   | --- | --- |
   | 新人教育 | カード・SOP・チェックリスト |
   | 外注 | カード・SOP・発注書・品質基準 |
   | 頭の整理 | カード・SOP・仕分け表 |
   | 求人 | カード・求人用 |
   | AI化準備 | カード・SOP・仕分け・AI化メモ |

6. `[要確認]` だけを埋める穴埋め（追加質問は最大5相当）
7. 第10章相当の QA。曖昧語・完了条件・禁則・金/安全のAI単独を弾く
8. 印刷、Markdown 書き出し
9. 施工店「請求まわす」の完成デモ（数字は架空）
10. 複数業務が混ざったら分割提案。次の1業務を1つだけ出す
11. PWA としてホーム画面に置ける（standalone）
12. 保存は端末内（プロトタイプは localStorage）。デモは消さない

内部エージェントはユーザーに見せない。見せるのは会話と成果物だけ。作業ログはタブ「ログ」に残してよい。

---

## 2. 本番 Web アプリで足すもの

### P0 公開前

| ID | 要件 | 理由 |
| --- | --- | --- |
| A1 | 会社（テナント）単位のアカウント | 業務は会社の資産。端末ローカルでは消える |
| A2 | ログイン（メールマジックリンクまたはパスワード） | 社長が現場の別端末からも開ける |
| A3 | 業務オブジェクトを DB に保存 | `schema/work.schema.json` を正本。成果物 Markdown は生成物 |
| A4 | ユーザーの素材・会話を学習に使わない | DESIGN 12.2。契約と実装の両方で止める |
| A5 | LLM キーはサーバのみ。ブラウザに出さない | 現行どおり |
| A6 | 税務・労務・安全を断定しない。必要な箇所は「専門家確認」 | 品質ゲート 10.3 |
| A7 | 個人の実名、病歴、賃金個別額を成果物に残さない | 禁止事項 |
| A8 | 本番障害時も取材を止めない。失敗したら「書いてください」に戻す | 8割で出す |

### P1 Phase 1 完成

| ID | 要件 | 画面の置き場 |
| --- | --- | --- |
| B1 | 録音のサーバ側文字起こし（Whisper 系）。話者分離は初期不要 | 取材のマイク。UI は変えない |
| B2 | メモ貼付（長文・LINE エクスポート） | 取材の入力。ホームにカードを増やさない |
| B3 | ファイル（PDF / xlsx / 画像）。原本はオブジェクトストレージ | 取材の入力。ホームに置かない |
| B4 | 共有リンク。閲覧のみ / 発注書ビューのみ | 成果物末尾の「書き出す」の隣に「共有」を1語足してよい |
| B5 | 印刷・PDF | 既存の印刷画面 |
| B6 | 権限: 会社メンバーは全成果物、外注先は発注書ビューだけ | DESIGN 12.2 |
| B7 | 取材と分解と QA は別 LLM コール | 現行どおり。混ぜない |
| B8 | `[要確認]` 初稿12個以下、改訂後3個以下を計測して残す | 評価指標 12.4 |
| B9 | 次の1業務の提案を、同じ会社の新規ドラフトに1タップで渡せる | 成果物末尾の「次は …」 |
| B10 | モバイル PWA（ホーム画面、standalone、safe-area） | 現行どおり |

### P2 以降

| ID | 要件 |
| --- | --- |
| C1 | 業種パック（内装、設備、整骨、士業補助） |
| C2 | 会社 / 現場の切替画面（12.1 の「会社 / 現場」） |
| C3 | 言語化した業務数課金（目安: 1業務 月1,500〜3,000円） |
| C4 | 改訂履歴（誰が、いつ、なぜ） |
| C5 | 検索（現場語で業務を探す） |
| C6 | 話者分離つき文字起こし |

課金UIはフロントの本線に載せない。設定か別画面。

---

## 3. 機能要件（振る舞い）

### 3.1 受付

- 業務名だけ → 取材へ。完了条件から聞く
- 長文・音声・ファイル → 要約せず本線を再構成する。不足は `[要確認]`
- 「この1業務だけ言語化して」→ 止めない。空欄を作って出す
- 明らかに複数業務 → 分割提案。今回は1つだけ切る

### 3.2 取材

- 質問は同時に最大3。6以上禁止
- 順: 呼び名 → 完了条件 → 本線の1件 → 例外3 / 失敗2 / 禁則3
- 禁止質問: 「御社の強みは？」「ビジョンは？」「DXの課題は？」
- 現場語を消して綺麗な日本語に置換しない。併記は可
- 例外から入らせない

### 3.3 分解・仕分け・執筆

ステップごとに `route` を付ける。複数可。

- `human_required` … 資格、対人感情が本丸、最終金額・契約・安全、失敗が重大
- `outsourceable` … 入出力が定義できる、第三者が合格を見られる
- `ai_capable` … 下書き・分類・転記・要約。最終送信・最終承認の前まで

成果物は第8章の見出しを維持する。不明でも見出しを消さず `[要確認]`。

推測で埋めた箇所は必ず `[推論]`。

### 3.4 穴埋めと QA

- 穴埋めは `[要確認]` と `[推論]` の一覧だけ。新しい質問を増やして迷子にしない
- QA は最終稿の前に必ず通す。落ちたら自己修正してから見せる
- 曖昧語を残さない: 適宜 / きちんと / しっかり / なるべく早く / 必要に応じて
- チェックリストは20項目以内。超えたら業務分割を提案
- 金の最終移動・契約送信・安全合図を AI 単独にしない
- 禁則は3つか「禁則なし（理由）」

### 3.5 状態

| 状態 | 表示 | 条件 |
| --- | --- | --- |
| draft | 途中 | 要確認が12超、または QA 指摘が重い |
| holes | 穴 | 要確認が残る |
| ready | 済 | 要確認0かつ QA 指摘0 |

デモ「請求まわす」は最初から済。削除不可。数字は架空と明記。

---

## 4. データ

1業務 = 1オブジェクト。ユーザーに JSON は出さない（希望したときだけ書き出し）。

必須フィールド（欠いたら `[要確認]`）:

- 現場名 / 正式名 / 担当役割 / 目的
- 開始条件 / 完了条件
- 本線ステップ（action, actor, route）
- 判断（if / then / else / 見るもの / エスカレーション）
- 例外 / 禁則 / 失敗
- 正社員必須・外注・AI の渡し方
- 保存場所 / ファイル名規則 / 個人情報の扱い

保存単位（WorkRecord。JSON Schema は `work` 部分のみ）:

```
{
  "id": "uuid",
  "company_id": "uuid",
  "created_at": "ISO-8601",
  "updated_at": "ISO-8601",
  "usage": "onboarding | outsource | organize | hiring | ai_prep",
  "source": "talk | memo | file",
  "status": "draft | holes | ready",
  "work": { ... WataseruWork ... },
  "artifacts": {
    "card": "md", "sop": "md", "checklist": "md",
    "order": "md", "routing": "md", "quality": "md",
    "hiring": "md", "ai": "md", "log": "md"
  },
  "messages": [{ "id": "", "role": "assistant|user|system", "content": "", "at": "" }],
  "qa_notes": "",
  "confirm_count": 0
}
```

`company_id` は本番で必須。クライアントが送った値を信じない。

API の最小形（本番）:

```
POST /works                 ドラフト作成
GET  /works                 会社の一覧
GET  /works/:id             オブジェクト + 成果物 + 会話
POST /works/:id/messages    取材1ターン（Interview）
POST /works/:id/generate    分解+執筆+QA（Decompose/Write/QA）
POST /works/:id/fill        穴埋め再生成
POST /works/:id/share       閲覧 or 発注書ビューのリンク
POST /transcribe            音声 → テキスト
POST /files                 原本アップロード（学習利用オフ）
```

テナント境界をサーバで強制する。

---

## 5. AI コール分割（混ぜない）

| コール | プロンプト | 出力 |
| --- | --- | --- |
| Interview | `prompts/system_interview.md` | `{ phase, say, questions[≤3], split_proposal }` |
| Decompose + Route | `prompts/system_decompose.md` + `system_router.md` | Work オブジェクト |
| Write | `prompts/system_write.md` | 使わない。テンプレ描画はアプリ側（現行どおり）でもよい |
| Fill | 既存オブジェクト + 穴の答え | 更新された Work |
| QA | `prompts/system_qa.md` | 指摘。アプリ側ルールと二重でもよい |

現行プロトタイプは Interview / Decompose / Fill を LLM、Write と QA の一部をアプリ側。本番もこの分担でよい。長文1本で全部やらない。

モデルは長文コンテキスト。温度は低め（現行 0.2）。応答は JSON オブジェクトのみ。キーはサーバのみ。

---

## 6. 非機能

| 項目 | 要件 |
| --- | --- |
| 対象端末 | スマホ第一（390幅）。現場の社長が親指で使う |
| PWA | standalone、ホーム画面、テーマは紙色 `#f3efe6` |
| 文字サイズ | 入力は16px以上 |
| オフライン | 閲覧はできなくてよい。入力途中の下書きは失わない |
| 言語 | 日本語。敬体。短文 |
| アクセシビリティ | コントラスト AA、フォーカス可視、ボタン44px |
| ログ | プロンプトと素材は学習に回さない。保持期間を契約に書く |
| 障害 | LLM 失敗でも取材を閉じない |

---

## 7. やらないこと

- 求人媒体の代替
- 給与・社保・仕訳の確定計算
- 「AIで人を増やさず売上2倍」などの誇張、根拠のない時短パーセント
- 業種法令の断定（顧問行為）
- 補助金ありきの提案
- 採用すれば解決します、で終えること
- 汎用経営コンサル
- ダッシュボード・グラフ・KPI ホーム
- フロントの再デザイン、カードの山、使い道の説明文付きチップ

---

## 8. 受け入れ（Phase 1 の完了条件）

社長（従業員3〜30人、または一人事業主）が、知らない業務名を口またはメモで渡し、90分相当のやり取りのあと:

1. 目的、開始と完了、手順（判断つき）、禁則、渡し先区分が1業務分そろっている
2. 使い道に応じた必須成果物が第8章の見出しで出る
3. `[要確認]` と `[推論]` が残っていても隠されていない
4. 印刷または共有リンクで、その場にいない人へ渡せる
5. 同じ会社の次の1業務が1つ提案される
6. 「明日これを渡せる」と本人が言える（主観、必須）

机上の BLEU は見ない。

---

## 9. 移植メモ（現行プロトタイプ → 本番）

フロントを同じにする最短経路。見た目の正本は [`frontend/`](./frontend/) にある。

| ファイル | 中身 |
| --- | --- |
| `frontend/src/styles.css` | トークン |
| `frontend/src/components/app-shell.tsx` | ヘッダと下タブ |
| `frontend/src/routes/index.tsx` | ホーム |
| `frontend/src/routes/interview.$workId.tsx` | 取材 |
| `frontend/src/routes/works.index.tsx` | 一覧 |
| `frontend/src/routes/works.$workId.tsx` | 成果物・穴埋め |
| `frontend/src/routes/print.$workId.tsx` | 印刷 |
| `frontend/src/lib/wataseru/types.ts` | 型 |
| `frontend/src/lib/wataseru/templates.ts` | 第8章レンダラ |
| `frontend/src/lib/wataseru/qa.ts` | 第10章の一部 |
| `frontend/src/lib/wataseru/demo.ts` | 請求まわす |
| `frontend/src/lib/server/grok.ts` | Interview / Decompose のサーバ境界（参考） |
| `frontend/public/favicon.svg` `icon-192.png` `icon-512.png` | 判子「渡」 |

差し替えだけすればよい箇所:

- `zustand` + localStorage → 会社スコープの API（A1–A3）
- Web Speech のみ → 録音アップロード + 文字起こし（マイクボタンの見た目は維持。B1）
- 「書き出す」の横に共有リンクを1語（B4）

コピーしないもの（ホスト固有）:

- AuthProvider / PreviewHostBridge / `/__grok/manifest.webmanifest`
- 本番の PWA は自前の `manifest.webmanifest`（name: ワタセル、theme_color: `#f3efe6`、standalone）

スタックは問わない。画面契約を守ること。
